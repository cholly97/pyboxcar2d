requires:
pybox2d (https://code.google.com/p/pybox2d/wiki/GettingStartedManual)

run instructions:
build pyboxcar2d.py in python 2.7
saves are saved in a folder called “save”
editor-built cars are saved in a folder called “editor saves”
these folders will be created automatically on save if they don’t exist already